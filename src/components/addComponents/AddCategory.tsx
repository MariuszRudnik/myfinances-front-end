import React from "react";

export const AddCategory = () =>{
    return (
        <div>
            <h1>Add an expense category.</h1>
            <input type="text"/>
            <button>Save</button>
        </div>
    );
}